import 'package:calculator_app/view/Calculator_Home.dart';
import 'package:flutter/material.dart';

void main() {}

class Calculator_app extends StatelessWidget {
  const Calculator_app({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Homepage(),
    );
  }
}
